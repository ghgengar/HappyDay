from datetime import date

"""
HappyDay.py - «Поздравлятор»

Приложение для ведения списка дней рождения друзей, коллег, знакомых.

Возможности:
- Просмотр всего списка дней рождения с сортировкой по дате,
  разделением на текущие (сегодня/будущие) и прошедшие.
- Просмотр сегодняшних и ближайших дней рождения
  (ближайшие - первые 3 будущих дня рождения, включая сегодняшние).
- Добавление, удаление, редактирование записей.
- Автоматическое сохранение в текстовый файл при выходе.

Формат хранения одной записи:
    [ "Имя Фамилия:", "день", "месяц" ]
    где "день" - число от 1 до 31 (строка),
        "месяц" - название месяца в родительном падеже
                  из списка: январь, февраль, …, декабрь.

Файл данных: построчный, поля разделены пробелами.
Пример строки файла:
    Иван Иванов: 1 января

Класс BirthDay содержит всю логику приложения.
"""

class BirthDay(object):
    """
    Основной класс приложения "Поздравлятор".

    Атрибуты класса:
        months (list[str]): Список названий месяцев в родительном падеже
                            (общий для всех экземпляров).

    Атрибуты экземпляра:
        dates (list): Список записей о днях рождения,
                      каждая запись - список из 3 строк.
        choice (str): Последний введённый пользователем пункт меню.
    """

    months = ["января", "февраля", "марта", "апреля", "мая", "июня", "июля", "августа", "сентября", "октября", "ноября", "декабря"]

    def __init__(self, d):
        """
        Инициализирует экземпляр приложения.

        Параметры:
            d (list): Список записей о днях рождения (формат описан в модуле).

        Действия:
            - Сохраняет переданный список в self.dates.
            - Выводит текущий список с помощью self.talk().
            - Запускает главное меню self.first_menu().
        """
        self.dates = d
        self.talk(self.dates)
        self.first_menu()

    def delete_or_rename_dates(self):
        """
        Выводит список записей с номерами и запрашивает номер для удаления/редактирования.

        Возвращает:
            int: Номер выбранной записи (от 1 до len(self.dates)) или 0 при отмене.
        """
        # Код метода...
        pass

    def test_date(self, date):
        """
        Проверяет корректность записи о дне рождения.

        Параметры:
            date (list): Запись вида [имя_с_двоеточием, день_строка, месяц_строка].

        Примечание:
            Проверяет корректность ввода имени, фамилии и принадлежность месяца к self.months.
            В случае несоответствия вызывает перехватываемое исключение int("error)")
        """
        # Код метода...
        pass

    def day_today(self):
        """
        Возвращает текущую дату (месяц и день) без ведущих нулей.

        Возвращает:
            list[str]: [месяц_как_число, день] в виде строк.
                       Например, для 5 июня -> ["6", "5"].
        """
        # Код метода...
        pass

    def question(self):
        """
        Запрашивает у пользователя ввод и сохраняет его в self.choice.
        """
        # Код метода...
        pass

    def first_menu(self):
        """
        Отображает главное меню и обрабатывает выбор пользователя.

        Доступные пункты:
            1 - Показать весь список (вызов self.talk + self.second_menu)
            2 - Показать сегодняшние и ближайшие ДР
            3 - Добавить запись
            4 - Удалить запись
            5 - Редактировать запись
            0 - Сохранить и выйти

        При некорректном вводе рекурсивно вызывает себя.
        """
        # Код метода...
        pass

    def talk(self, dates):
        """
        Выводит список записей на экран (по одной строке).

        Параметры:
            dates (list): Список записей для вывода (каждая запись - список строк).

        Если dates пуст, выводит "<список пуст>".
        """
        # Код метода...
        pass

    def today_near(self, dates):
        """
        Выводит сегодняшние и ближайшие дни рождения.

        Параметры:
            dates (list): Отсортированный список записей (результат self.sorting).

        Логика:
            - Сегодняшние: совпадение месяца и дня с текущей датой.
            - Ближайшие: первые 3 записи из списка будущих ДР (self.future_past).
            - Если какой-то из списков пуст, выводится сообщение "<список пуст>".

        Результат выводится через self.talk.
        """
        # Код метода...
        pass

    def append_dates(self):
        """
        Добавляет новую запись в список self.dates.

        Последовательность:
            1. Запрашивает имя, фамилию, дату рождения.
            2. Проверяет ввод через self.test_date (при ошибке возвращается в главное меню).
            3. Запрашивает подтверждение (y/n).
            4. При подтверждении добавляет запись в self.dates и выводит сообщение об успехе.
        """
        # Код метода...
        pass

    def delete_dates(self):
        """
        Удаляет запись из списка self.dates.

        Последовательность:
            1. Вызывает self.delete_or_rename_dates() для выбора номера.
            2. Запрашивает подтверждение (y/n).
            3. При подтверждении удаляет запись и выводит сообщение.
        """
        # Код метода...
        pass

    def rename_dates(self):
        """
        Редактирует существующую запись в списке self.dates.

        Последовательность:
            1. Вызывает self.delete_or_rename_dates() для выбора номера.
            2. Запрашивает новые имя, фамилию, дату рождения.
            3. Проверяет ввод через self.test_date.
            4. Запрашивает подтверждение (y/n).
            5. При подтверждении заменяет запись и выводит сообщение.
        """
        # Код метода...
        pass

    def save_and_exit(self):
        """
        Сохраняет текущий список self.dates в файл и завершает программу.

        Формат файла: каждая запись - строка с пробельными разделителями.
        """
        # Код метода...
        pass

    def second_menu(self, sort=True, fp=True):
        """
        Отображает дополнительное меню (сортировка, текущие/прошедшие).

        Параметры:
            sort (bool): Показывать ли пункт "Сортировка".
            fp (bool): Показывать ли пункт "Текущие/Прошедшие".

        При выборе пункта вызывает соответствующий метод,
        при выборе "0" возвращается в главное меню.
        Рекурсивно вызывает себя при неверном вводе.
        """
        # Код метода...
        pass

    def sorting(self, dates, notalk=False):
        """
        Сортирует список записей по дате (месяц, день).

        Параметры:
            dates (list): Список записей для сортировки.
            notalk (bool): Если True, возвращает отсортированный список и не выводит его.

        Возвращает:
            list or None: Если notalk == True, возвращает отсортированный список.
                          Иначе выводит список через self.talk и возвращает None.

        Примечание:
            Сортировка выполняется на месте (изменяет переданный список).
        """
        # Код метода...
        pass

    def future_past(self, dates, notalk=False):
        """
        Разделяет записи на будущие (текущие) и прошедшие относительно текущей даты.

        Параметры:
            dates (list): Список записей для анализа.
            notalk (bool): Если True, возвращает список будущих записей.

        Возвращает:
            list or None: Если notalk == True, возвращает список будущих записей.
                          Иначе выводит разделённые списки через self.talk.

        Примечание:
            «Будущими» считаются записи, у которых месяц > текущего,
            или месяц == текущему и день >= текущему.
        """
        # Код метода...
        pass

def main():
    """
    Главная функция приложения.

    Открывает файл с базой данных,
    читает записи и преобразует их в формат, понятный классу BirthDay.

    Формат чтения:
        - Файл открывается в режиме "a+" (чтение + добавление).
        - Каждая строка разбивается по пробелам.
        - Если в строке 4 слова, значит имя было составным (например, "Анна Мария:"),
          второе слово присоединяется к первому.

    Инициализирует экземпляр BirthDay и запускает приложение.
    """
    # Код функции...
    pass